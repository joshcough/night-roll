# Session handoffs — the archive

Every web/iPad analysis session ends with a handoff file for Claude Code
(see WEB-SESSION.md, session shape step 4). They land on Josh's Desktop,
get applied, and are archived here verbatim — several contain the ONLY
record of a derivation path until it's transcribed into analysis/*.md
(e.g. prologue 6.3 in handoff-2026-08-14.md), so they are source
material, not disposable process files.

floating-castle-parked.md is NOT here — it's spoiler-quarantined in
albums/final-fantasy-i/reference/ with its own rules.

Claude Code: when Josh hands you a new handoff, **`mv` it here first** —
before applying it, and move rather than copy (Josh, 2026-08-25). The
move is what makes a duplicate detectable: if he hands you the same file
twice, the second time the Desktop path is simply gone and this
directory already has it. Check for an existing file of that name before
moving, and say so instead of overwriting. Then apply it.

Recovered 2026-08-15 from Desktop/Music: the two town handoffs from
2026-08-02 (the short verdict file and the extended "copy", both kept —
they show the thinking at the time) and the 08-02 session-notes
ADDENDUM. The base session-notes-2026-08-02.md it amends was applied
and discarded before this archive existed — the addendum is what
survives of it.
